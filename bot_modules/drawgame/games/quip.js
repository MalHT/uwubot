const common = require('./common.js');

let gameExports = {};

gameExports.rules = {
  maxplayers: 8
}

gameExports.startGame = function(game) {
  
  
  
};

module.exports = gameExports;